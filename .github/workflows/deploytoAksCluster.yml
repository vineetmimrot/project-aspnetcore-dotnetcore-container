on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    
    - uses: azure/docker-login@v1
      with:
        login-server: vnmacr.azurecr.io
        username: ${{ secrets.acr_vnmacr_username }}
        password: ${{ secrets.acr_vnmacr_password }}
    
    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/aspnet-core-dotnet-core" -f  "aspnet-core-dotnet-core/Dockerfile" -t vnmacr.azurecr.io/vnmpoc46:${{ github.sha }}
        docker push vnmacr.azurecr.io/vnmpoc46:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      with:
         kubeconfig: ${{ secrets.aks_vnmpoc46_kubeConfig }}
      id: login
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep vnmpoc46a4cd | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace vnmpoc46a4cd`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: vnmpoc46a4cd
        container-registry-url: vnmacr.azurecr.io
        container-registry-username: ${{ secrets.acr_vnmacr_username }}
        container-registry-password: ${{ secrets.acr_vnmacr_password }}
        secret-name: vnmpoc46dockerauth
       
    - uses: azure/k8s-deploy@v1
      with:
        namespace: vnmpoc46a4cd
        manifests: |
          manifests/deployment.yml
          manifests/service.yml
        images: |
          vnmacr.azurecr.io/vnmpoc46:${{ github.sha }}
        imagepullsecrets: |
          vnmpoc46dockerauth